# 实验2.1a: 增加CAM损失权重
# 从0.5增加到2.0

# Model configuration
surgery_clip_checkpoint: "checkpoints/RemoteCLIP-ViT-B-32.pt"
num_classes: 20
cam_resolution: 7
upsample_cam: false

# Training configuration
device: "cuda"
num_epochs: 20  # 快速测试
batch_size: 8
learning_rate: 1.0e-4
cam_generator_lr: 1.0e-5
weight_decay: 0.01

# Loss configuration - 实验2.1a: 增加CAM损失权重
lambda_l1: 1.0
lambda_giou: 2.0
lambda_cam: 2.0  # 从0.5增加到2.0

# Multi-instance assigner configuration
min_peak_distance: 2
min_peak_value: 0.3
match_iou_threshold: 0.3

# Data configuration
dataset_root: null
num_workers: 4
image_size: 224
augment: true
train_only_seen: true

# Logging and saving
log_interval: 50
save_interval: 10
checkpoint_dir: "checkpoints/exp2.1a"
output_dir: "outputs/experiments/exp2.1a"

# Evaluation configuration
conf_threshold: 0.1
nms_threshold: 0.5


